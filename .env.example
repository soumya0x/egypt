# Server
PORT=3000
NODE_ENV=development

# Admin auth (CHANGE in production)
ADMIN_USER=admin
ADMIN_PASS=pharaoh2026

# Postgres connection (Neon). Leave empty to use local node:sqlite.
DATABASE_URL=

# SMTP for outbound email (optional — leave blank to log only)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
FROM_EMAIL=noreply@egypt-itinerary.local
TO_EMAIL=hello@youregypt.example
